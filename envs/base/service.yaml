apiVersion: v1
kind: Service
metadata:
  name: service  
spec:
  selector:
    app: app
  ports:
    - name: http-app
      port: 8080
      targetPort: 8080
      appProtocol: http
    - name: http-admin
      port: 8081
      targetPort: 8081
      appProtocol: http